import {
    cachedBasketballDataType,
    cachedBoxscoreDataType,
} from '../types/basketballdata';

export const cachedGameData = {} as cachedBasketballDataType;
export const cachedBoxscoreData = {} as cachedBoxscoreDataType;
